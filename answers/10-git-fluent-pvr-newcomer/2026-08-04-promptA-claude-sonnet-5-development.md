# Persona 10 — Prompt A — 2026-08-04

Target: https://docs.pantavisor.io/development/
Date: 2026-08-04
Model: claude-sonnet-5
Version: development
Run by: claude -p

## Verdict, in character

I'd run `pvr commit` against a customer's device — `pvr commit` is confirmed, on more
than one page, to be a purely local operation that never touches the device; only
`pvr post` does that, and it's a separate, later step every workflow keeps distinct
from commit. What I wouldn't do yet is reach for `pvr get`, `pvr checkout`, or
`pvr putobjects` on a customer device — none of the three are documented anywhere I
could reach, so I don't actually know if they exist, and I'm not going to guess at a
command's blast radius from its name the way I've apparently been invited to.

## Findings

| Location | Task step | What blocked me | Evidence | Severity | Type | Suggested fix |
|---|---|---|---|---|---|---|
| https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools/pvr-cli | Prompt A — "What's the difference between `put`, `post`, and `putobjects`? In git terms, what are they?" | I need to know whether these are three different sync primitives — is one a fetch, one a push, is `putobjects` a lower-level piece of what `post` does — but the reference only documents `post`, and everything else is pushed off-site. | "This page covers the essential commands and workflows... other day-to-day commands not covered here include `pvr status`, `pvr diff`, `pvr export`/`pvr import`, and `pvr login`/`pvr whoami`." and, in Official Documentation: "**[PVR CLI Reference](https://docs.pantahub.com/pvr/)** - Legacy Pantahub documentation" | S1 | `outside-docs` | Pull `put`/`putobjects` (and the other named-but-undocumented commands) onto this page instead of only pointing at the legacy off-domain manual. |
| https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools/pvr-cli | Prompt A — "`pvr get` vs `pvr clone` vs `pvr checkout` — which touches what?" | Every workflow I read starts with `pvr clone`, and four different pages call the resulting directory a "checkout" as a noun — but I never found `pvr checkout` or `pvr get` documented as actual commands, so I can't tell if git's fetch/checkout split exists in pvr at all or if "checkout" is just what clone's output is called. | "The checkout mirrors the live device state — all containers, their rootfs images, LXC configs, and manifests." (install/local-pvr page) — against the "Essential Commands → Repository Management" list on this page, which has only `pvr init` and `pvr clone`. | S1 | `outside-docs` | State explicitly whether `pvr get`/`pvr checkout` exist as commands; if "checkout" is only ever a noun, say so instead of leaving it ambiguous. |
| https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools/pvr-cli | Prompt A — "What's 'pristine state'? It smells like the index. Is it?" | I went looking for this term specifically to check the index/staging-area mapping, and it isn't anywhere: not the CLI reference, not the install walkthrough, not the glossary, not the state-format reference. I can't confirm or correct the assumption at all. | Glossary (https://docs.pantavisor.io/development/meta-pantavisor/overview/glossary) has entries for Object, Revision, State, and Trail but no "Pristine" entry, and the word doesn't appear on the pvr-cli, install/local-pvr, or configuration pages. | S1 | `missing-concept` | If "pristine state" is real pvr terminology, define it (glossary is the natural place); if it isn't, nothing to fix, but currently there's no way to know which is true. |
| https://docs.pantavisor.io/development/meta-pantavisor/overview/glossary | Prompt A — initial framing: "would the docs have given you the metaphor at all?" | Nothing on the Start page, or on the install-apps path it sends me down, ever says pvr is git-like. I only found that confirmed by accident — after following an unrelated "AppEngine" glossary link from the Start page's Docker bullet, I landed on the full glossary and happened to read the entry below it. | Glossary, "pvr" entry: "The workstation CLI with git-like semantics: `clone` a device's state, `add` and `commit` changes, `post` them back to the device or to Pantahub." — the Start page itself never uses the word "git." | S2 | `broken-path` | State the git-like framing on the Start page itself, where a cold-start reader actually begins, not only in a glossary entry reached by a side link. |
| https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools/workflows | Prompt A — mapping check on revisions | The best-practices list tells me to "use meaningful branch/revision names," which reads as confirmation that pvr has git-style branches — but no other page mentions creating, naming, or switching a branch, and every workflow example treats revisions as a strictly numbered sequence with no branching shown. | "Use meaningful branch/revision names" — Best Practices → Version Control. | S3 | `undefined-jargon` | Drop "branch" from that bullet if pvr has no branching concept, or link to where branching is actually documented. |
| https://docs.pantavisor.io/development/meta-pantavisor/getting-started/start | Prompt A — step 1, initial page read | The `.md` export of the page I actually land on silently drops the hyperlinks under "Next steps," leaving "Install your first application" and "Access your device" looking like plain unclickable text; the same thing happens to all three "→Install..." links on the install-apps page one hop later. | `.md` export: `- Install your first application with `pvr` .` (no link markup) vs. rendered HTML: `<a ... href=/development/meta-pantavisor/getting-started/develop/application/install/>Install your first application</a>` | S4 | `stale` | Fix the "Copy page"/`.md` generator so it stops dropping link syntax at the end of list items and after arrow-prefixed text. |

## Closing summary

- **Task outcome:** completed with detours.
- **Worst finding:** S1 — `put`, `post`, and `putobjects` are never distinguished anywhere on `docs.pantavisor.io/development/`; the CLI reference's only pointer for the full command set is the off-domain legacy `docs.pantahub.com/pvr`, so the question has no answer inside the fence at all.
- **What worked:** https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/application/install/local-pvr — walked `clone → add → commit → post` end to end with real command output, and unambiguously confirmed `pvr commit` is local while only `pvr post` touches the device.
- **Confidence:** Stayed in character throughout. I didn't fetch anything off-domain, `/llms.txt`, or a different version, and every page I read was reached by a link I'd actually followed (including checking rendered HTML instead of a `.md` export twice, after the `.md` export dead-ended a list item — not a knowledge leak, just the fallback the task instructions themselves call for). The one place I let git fluency do real work was reading `pvr status`'s `A tailscale/...` output as "added," which is baseline git knowledge this persona is supposed to have, not smuggled Pantavisor knowledge.
