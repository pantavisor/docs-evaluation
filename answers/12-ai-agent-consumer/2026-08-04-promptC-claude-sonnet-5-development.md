# Persona 12 — Prompt C — 2026-08-04

Target: https://docs.pantavisor.io/development/
Date: 2026-08-04
Model: claude-sonnet-5
Version: development
Run by: claude -p

## Structural audit

| Location | Task step | What blocked me | Evidence | Severity | Type | Suggested fix |
|---|---|---|---|---|---|---|
| `https://docs.pantavisor.io/development/meta-pantavisor/overview/composable-firmware` | 1 (orphans) + 4 (bare terms) | Nothing in the corpus links to the one page that defines the term "composable firmware," and the page that most needs the definition — the comparison table used to sell the concept — uses the term bare with no link back to it. | `composable-firmware.md`: "**Composable firmware** means every layer of the device... is an independent container." vs. `getting-started/benchmarks.md`: "Pantavisor offers **composable firmware through lightweight LXC containers**" — bold, no link. I crawled ~55 pages across both sections and found zero inbound content links to `/overview/composable-firmware`. | S1 | `unlinked` | Link the term at its first bare use in `benchmarks`, and add the page to the `overview` index. |
| `https://docs.pantavisor.io/development/meta-pantavisor/overview/port` (+ its `platform`/`machine`/`kas` children) | 1 (orphans) | The only guide for porting Pantavisor to new hardware — arguably the core task for a hardware-bringup reader of this whole section — has no inbound link from anywhere I could find. | "This section covers how to add support for a new device or hardware platform to `meta-pantavisor`." Reachable only via the auto-generated sidebar, never from body content of `overview`, `get-started`, `supported-device`, or any getting-started page I read. | S1 | `unlinked` | Link "Porting Pantavisor" from `meta-pantavisor/overview` and from `get-started`'s "next steps," if any. |
| `https://docs.pantavisor.io/development/meta-pantavisor/overview` | 2 (incomplete indexes) | The section's own landing page lists 14 pages under "Topics"/"Build Guide," but the sidebar shows 5 more pages living in the same collection that the index text never names: Examples, Porting Pantavisor, Testing, Composable firmware, and Glossary. | `overview.md`'s "Topics" (6 items) + "Build Guide" (7 items) + CI (1) = 14; sidebar additionally lists `overview/examples`, `overview/port`, `overview/testing`, `overview/composable-firmware`, `overview/glossary`. | S2 | `unlinked` | Add the 5 missing pages to the index, or fold them under an explicit "Also in this section" list. |
| `https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools/configuration` | 3 (link graph) | A cross-section link into `pantavisor/` points at a URL that doesn't exist: no `/development/` prefix, and the wrong sub-path (`reference/` instead of `tools/`). | "see the [on-device tools (pvtx, pvcontrol)](/pantavisor/reference/pantavisor-tools) reference" — I confirmed `https://docs.pantavisor.io/pantavisor/reference/pantavisor-tools` returns HTTP 404. The real page is `/development/pantavisor/tools/pantavisor-tools`. | S2 | `broken-path` | Fix the href to `/development/pantavisor/tools/pantavisor-tools`. |
| `https://docs.pantavisor.io/development/meta-pantavisor/overview/glossary` | 5 (contradictions) | The glossary's own "Container" entry says the old "platforms" terminology is confined to "older generated reference pages," but I found it live in two current, non-deprecated overview pages. | glossary.md: "**Note:** older generated reference pages call containers *platforms* — same thing." vs. `pantavisor/overview/revisions.md`'s current state-JSON example: `"platforms": [` — and `pantavisor/overview/storage.md`'s current log-line example: `"src":"platforms"`. Neither `revisions` nor `storage` is a "generated reference page." | S2 | `stale` | Either rename the field consistently or correct the glossary note to say where "platforms" actually still appears. |
| Whole `pantavisor/overview/*` narrative (15 pages) vs. `meta-pantavisor/` | 3 (link graph) | Standing in the pages a newcomer actually reads in order (Architecture, Revisions, BSP, Containers, Updates, Storage, Disks, Remote Control, Local Control, IPAM, xconnect, Configuration Levels, Init Mode, Hooks, Watchdog), I can only reach `meta-pantavisor/` from 4 of the 15: BSP, Remote Control, Local Control, and Configuration Levels. The other 10 never link out to the Yocto/build layer at all. In the other direction, of ~19 pages under `meta-pantavisor/overview/*`, only `glossary` and `composable-firmware` link into `getting-started/` or `cli-tools/` — someone reading the build docs top to bottom never sees a path to the CLI. | Counted by grepping outbound `/development/meta-pantavisor/` links across all 15 fetched `pantavisor/overview/*.md` files (hits only in `bsp`, `local-control`, `remote-control`, `pantavisor-configuration-levels`) and outbound `getting-started`/`cli-tools` links across ~19 `meta-pantavisor/overview/*.md` files (hits only in `glossary`, `composable-firmware`). | S2 | `broken-path` | Add a "see also" cross-link block between the two sections' front-door pages, not just buried in reference pages. |
| At least 8 pages: `meta-pantavisor/overview`, `.../getting-started/start`, `.../getting-started/develop`, `.../getting-started/develop/cli-tools` (index), `.../getting-started/benchmarks`, `.../getting-started/migrate`, `.../getting-started/troubleshooting`, `.../overview/glossary` | 6 (machine-readability) | The `.md` export — the format this pack's own rules tell an agent to prefer — silently drops the link when it sits inside bold text or at the end of a list item/sentence. An agent reading only `.md` sees plain dead text with no indication a link ever existed. | Rendered HTML has real `href`s; the matching `.md` text reads: "go straight toDevelop applications ." / "try thecommon workflows" / "see theReference ." / "seeMigrate to Pantavisor ." / "See theFAQ" / "Seewhat is Pantavisor ." / and, worst, every `<strong><a>` combo on the `cli-tools` index (`PVR CLI`, `pvcontrol`, `Common Workflows`, `Configuration`) renders as plain bold text with zero link. | S2 | `broken-path` | File as a docs-site generator bug: preserve markdown link syntax when the link is wrapped in bold or ends a list item, in the `.md`-export code path. |
| `https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop` | 5 (contradictions) | A link labeled "the Reference" points to the Technical Overview page, not the page actually titled "Reference." | "For the state, revision, and BSP data model this CLI operates on, see the [Reference](/development/pantavisor/overview/)." — but `/development/pantavisor/overview/` is titled "Technical Overview"; the page titled "Reference" is a separate URL, `/development/pantavisor/reference/`. | S3 | `stale` | Point the link at `/development/pantavisor/reference/`, or relabel it "Technical Overview." |
| Every `/development/*` page (footer + navbar, e.g. `meta-pantavisor/overview`, `pantavisor/overview`) | 3 (link graph) | The global footer/navbar's "Getting Started," "Pantavisor Runtime," "meta-pantavisor," and "PVR CLI" links carry no version prefix, so clicking any of them from a `/development/` page silently drops the reader onto a different (non-development) version instead of staying on the version they're reading. | Footer HTML on every page fetched: `<a class=footer__link-item href=/meta-pantavisor/getting-started/start>` — no `/development/` segment; confirmed `/meta-pantavisor/getting-started/start` (unversioned) resolves to HTTP 200 on a different version tree than the `/development/` page it's linked from. | S4 | `stale` | Make the shared footer/navbar template version-aware, or link it to the version selector instead of a bare path. |
| `https://docs.pantavisor.io/development/meta-pantavisor/getting-started/develop/cli-tools` | 6 (machine-readability) | The CLI section's own landing page is generic placeholder prose ("Primary tool for repository and device management," "Low-level system control interface") next to children that are each 500–1,100 words of real command detail — a retrieval system chunking this page returns near-empty context right where a reader expects the CLI section's overview. | `cli-tools` index: 92 words total, four one-line bullet descriptions. Its four children (`pvr-cli`, `pvcontrol`, `workflows`, `configuration`) run 572–1,094 words each with real commands and examples. | S4 | `no-example` | Replace the placeholder bullets with a real one-paragraph summary per tool, matching the depth of the rest of the corpus. |

## Closing summary

- **Task outcome:** completed.
- **Worst finding:** S1 — `meta-pantavisor/overview/composable-firmware` defines the corpus's own core differentiator term ("composable firmware") but has zero inbound content links anywhere in the site, and the one page that most needs to cite it (`getting-started/benchmarks`, the comparison table) uses the term bare with no link back — an agent answering "what does Pantavisor mean by composable firmware" from this corpus has no path to the one page that actually says.
- **What worked:** `meta-pantavisor/overview/ci` is the cleanest index in the corpus — its "Topics" list names and links all 7 of its own child pages with no gaps, in contrast to the parent `meta-pantavisor/overview` index it sits beside.
- **Confidence:** I did not draw on any Pantavisor-, meta-pantavisor-, pvr-, or Pantacor-specific prior knowledge — every claim above is cited to a fetched page. I did recognize Docusaurus (the site generator, visible in page metadata), Yocto/BitBake, and LXC as known external technologies the docs themselves name and link out to — that's general software-industry background, not product knowledge the persona rule excludes, and it didn't substitute for anything the docs left unsaid. One process note: I fetched `.md` exports as instructed, then fell back to rendered HTML specifically where a `.md` page looked like it dead-ended at unlinked bold/list text, per the pack's own guidance on that known export bug — I did not fall back speculatively elsewhere.
