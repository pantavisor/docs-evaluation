# Persona 07 — Prompt B — 2026-08-04

Target: https://docs.pantavisor.io/development/
Date: 2026-08-04
Model: claude-sonnet-5
Version: development
Run by: claude -p

## Findings

| Location | Task step | What blocked me | Evidence | Severity | Type | Suggested fix |
|---|---|---|---|---|---|---|
| `https://docs.pantavisor.io/development/pantavisor/overview/bsp` | B, step 1 (root of trust) | I need the chain from silicon to userspace, but the docs only describe Pantavisor's own post-boot artifact/signature check ("secureboot") — nothing verifies the bootloader or the Pantavisor initrd binary *before* they execute, and no hardware root of trust (fused keys, HAB, etc.) is mentioned anywhere. | "Pantavisor is solely responsible for writing the target revision into the bootloader environment; the bootloader only reads from it at boot time to locate the kernel and initrd to load." — the 6-step boot sequence on `meta-pantavisor/overview/boot-flow` likewise contains no verification step before `bootm`/`booti`. | S1 | `missing-concept` | State explicitly whether U-Boot/GRUB and the Pantavisor initrd are authenticated before execution and, if so, by what silicon mechanism — or say plainly that this is left to the integrator. |
| `https://docs.pantavisor.io/development/pantavisor/overview/containers` | B, step 4 (container isolation) | Containers aren't a security boundary by default, so I need the confinement story, but the docs describe rootfs, volumes, groups, roles and restart policy for LXC containers and never mention seccomp, AppArmor, or Linux capabilities — the `run.json` schema on the state-format reference has no such field either. | Full page content enumerates Storage, Networking, Groups, Roles, Restart Policy, Status, Auto-Recovery, Drivers, Loggers, Service Mesh — no confinement-mechanism section exists. | S1 | `missing-concept` | Document what confinement beyond namespaces/rootfs (if any) is applied to LXC containers by default, or state that none is and it's left to the integrator's LXC config. |
| `https://docs.pantavisor.io/development/meta-pantavisor/getting-started/operate/device-access/remote-pantahub` | B, step 5 (device identity) | I can see *how* claiming works but not what the credential cryptographically *is* or what stops it being copied to a second device. | "Read the device ID and challenge token from the serial console debug shell... `cat /pv/device-id` / `cat /pv/challenge`" — paired with `PH_CREDS_SECRET` in the configuration reference, described only as "set Pantacor Hub credentials secret," with no mention of TPM/secure-element binding or anti-cloning measures. | S1 | `missing-concept` | Document what the device-id/challenge/`PH_CREDS_SECRET` triplet actually is (random token vs. hardware-derived) and what, if anything, binds it to one physical device. |
| `https://docs.pantavisor.io/development/meta-pantavisor/overview/manifest-audit` | B, step 6 (SBOM/CVE) | I need to know if I can produce an SBOM and who to contact for a CVE, but the only build-integrity-adjacent page audits rootfs diffs against a reference manifest for accidental changes — it has nothing to do with a bill of materials, and no page in either section names a security contact or disclosure process. | Page covers only: "Audits rootfs filesystem entries for deterministic changes... Compares against reference manifests using unified-diff patches." No SBOM, CVE, or disclosure-process content found on the CI page or anywhere else reachable from either overview. | S1 | `missing-concept` | Add a security-contact / disclosure-process page (and note SBOM generation status, even if "not currently supported") linked from the pantavisor or meta-pantavisor overview. |
| `https://docs.pantavisor.io/development/meta-pantavisor/overview` | B, step 6 (navigation, while looking for CI/SBOM info) | The "Continuous Integration" list item under the `.md` export has no link at all, so it looks like a dead-end unless I check the rendered page. | `.md` export: "Continuous Integration — CI system overview, machine matrix, release builds, tag sync, and docs publishing" (no markdown link syntax); rendered HTML confirms a working link to `/development/meta-pantavisor/overview/ci/`. | S4 | `stale` | Fix the `.md` export for this list item so it emits the link like its sibling topics do. |

## Closing summary

- **Task outcome:** completed — the trust-model assessment could be written from the docs alone, with four sections legitimately marked NOT DOCUMENTED per the task's own instructions (root of trust chain to silicon, container confinement mechanism, device-identity/anti-cloning binding, SBOM/CVE process). I never had to leave `docs.pantavisor.io/development/` to reach this conclusion.
- **Worst finding:** the "secureboot" feature name is misleading for a security reviewer — it is Pantavisor's own post-boot artifact/signature validation, not a hardware chain of trust, and nothing in the docs verifies the bootloader or the Pantavisor binary itself before they run, which is exactly the gap this persona cannot sign off past.
- **What worked:** `pantavisor/overview/storage` ("Integrity" section) together with `pantavisor/reference/pantavisor-state-format-v2` (section 9, `pvs@2`) fully specified the update-signing scheme in one hop each — algorithms (RS256/ES256/ES384/ES512), verification points, on-disk-key vs. certificate-chain trust modes, and exact failure behavior (WONTGO/ERROR → rollback or reset). `pantavisor/overview/updates` also documents anti-downgrade protection explicitly ("Stale revision: An revision was found in hub with an index that is less than the current running revision one" → ERROR).
- **Confidence:** stayed in character. I used the persona's own known vocabulary (HAB, TPM, seccomp/AppArmor, secure element) only to name what I was looking for and failed to find — I never treated any of that vocabulary as something the docs themselves had said, and I did not infer undocumented Pantavisor-specific mechanisms from general security knowledge.
